{
  "name": "kairos",
  "version": "0.1.0",
  "private": true,
  "description": "Kairos — an authenticity-preserving career representation engine (monorepo: shared engine + local lane; cloud lane lands in apps/cloud)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm -w kairos-local run dev",
    "build": "npm -w kairos-local run build",
    "start": "npm -w kairos-local run start",
    "mcp": "npm -w kairos-local run mcp",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p packages/engine && tsc --noEmit -p apps/local && tsc --noEmit -p apps/cloud",
    "eval:calibration": "tsx evals/run-calibration.mts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.23.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
